name: release-please

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v5
        id: release
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      # Everything below only runs when a release PR was just merged.
      - name: Checkout Repository
        if: ${{ steps.release.outputs.release_created }}
        uses: actions/checkout@v6

      - name: Set up Bun
        if: ${{ steps.release.outputs.release_created }}
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: .bun-version

      # vendor/ is gitignored and package.json depends on it via
      # file:./vendor/mochi-framework, so vendor it before publishing.
      - name: Vendor mochi-framework and install dependencies
        if: ${{ steps.release.outputs.release_created }}
        run: bun run update:mochi

      # Ship a production build: Mochi only serves static assets (the chime WAVs)
      # from a prebuilt manifest, so `.mochi/` must be built and packaged.
      - name: Build
        if: ${{ steps.release.outputs.release_created }}
        run: bun run build

      - name: Configure npm auth
        if: ${{ steps.release.outputs.release_created }}
        run: echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc

      - name: Publish to npm
        if: ${{ steps.release.outputs.release_created }}
        run: bun publish --access public
